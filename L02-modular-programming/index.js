const math = require("./math");
// const { addFn, subFn } = require("./math") //Or we can do destructuring here

console.log("Math value is ", math.addFn(2, 3));
// console.log("Math value is ", math.subFn(2, 3));